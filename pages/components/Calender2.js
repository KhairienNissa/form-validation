import React from 'react'

function Calender2() {
  return (
    <div> <input className="border text-sm p-1 w-full" type="date"
    onChange={({ value }) => form.setFieldValue(field.name, value)}
    max={max}
    
    /></div>
  )
}

export default Calender2